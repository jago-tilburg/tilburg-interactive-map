{
  "name": "functions",
  "description": "Server-authoritative writes for 2happies.nl (staging) — event approval and mock payment confirmation. Nothing here runs against prod yet.",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2"
  },
  "private": true,
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "vitest": "^4.1.11"
  }
}
