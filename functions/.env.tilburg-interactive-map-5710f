# Deploy-time params for the staging project (tilburg-interactive-map-5710f).
# Not secrets — these are public values; real secrets live in Secret Manager
# via defineSecret (see STRIPE_SECRET_KEY / STRIPE_WEBHOOK_SECRET in index.js).
#
# APP_BASE_URL must live here, not only as defineString's `default`: the
# firebase CLI ignores that default on a non-interactive deploy and hard-fails
# with "no value for the following environment variables: APP_BASE_URL".
APP_BASE_URL=https://tilburg-2happies-staging-next--tilburg-interactive-map-5710f.europe-west4.hosted.app
